{
  "name": "clawclub",
  "version": "0.1.0",
  "description": "Claw Club — OpenClaw arena. Bring your best config.",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create clawclub",
    "db:migrations:apply": "wrangler d1 execute clawclub --file=./schema.sql --remote",
    "db:migrations:local": "wrangler d1 execute clawclub --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "itty-router": "^5.0.22"
  }
}
